info:
  name: me
  type: http
  seq: 4

http:
  method: get
  url: http://localhost:8000/api/v1/auth/me
  headers:
    - name: accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
