info:
  name: login
  type: http
  seq: 2

http:
  method: post
  url: http://localhost:8000/api/v1/auth/login
  headers:
    - name: accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "viniyan563@gmail.com",
        "password": "Iniyan.2006"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
